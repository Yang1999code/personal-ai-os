# 元loop 修改记录

这个文件记录 `04-meta-loop` 的重要修改，方便人和 Agent（智能体）复盘。

## 2026-06-16 - 初始化元loop骨架

- 改前：`myloop` 里只有 `research/create/test` 三个场景 Loop，没有专门学习“人的处理方式”的场景 Loop。
- 改后：新增 `04-meta-loop`，采用“总控 + 四个子循环”的结构：回放、意图推断、规则抽象、对齐。
- 原因：把“学人而不是学工具”的需求单独建模，避免硬塞进 skill 或普通执行 Loop。
- 隐患：目前还是第一版骨架，真实会话输入、稳定规则格式、跨任务验证方式后续还需要继续迭代。
- 验证：检查 `README.md`、`loop.md`、`99-skills/recommended-skills.md` 和四个子循环说明文件存在。
- git：待提交。
