# Decisions

这里记录 Loop（循环）运行中的重要决策。

只记录会影响后续多轮判断的内容，不需要写流水账。

## 记录模板

```text
时间：
决策：
原因：
影响：
是否需要未来复查：
```

## 当前决策

- 采用轻脚手架、信任优先的设计。
- 运行模式默认自由模式，但保留协作模式和自动模式。
- 验证方式由 AI 根据具体目标在 `09-target/verification.md` 中维护。

