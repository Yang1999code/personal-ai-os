# First Run Prompt

可以把下面这段直接发给 AI：

```text
读取 $HOME/Desktop/myloop/ai-loop-mini/loop.md，
按这个 Loop 跑一轮。

本轮目标：
检查这个 Loop 工作区本身是否清楚、可执行、轻脚手架、信任 AI，并更新状态、历史、输出和优化记录。

要求：
- 主动判断最值得检查的点
- 必要时使用 Maker / Tester 视角
- 检查运行模式是否合适
- 如果目标需要验证，更新 `09-target/verification.md`
- 不要大改结构，除非发现明显问题
- 每轮结束后更新记录
```
