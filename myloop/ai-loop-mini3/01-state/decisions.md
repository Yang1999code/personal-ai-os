# Decisions

这里记录会影响后续判断的重要决策。

## 当前决策

- 本 Loop（循环）不追求全自动，而是在高质量深度探索外增加一个很薄的 while 执行层。
- 默认最多三轮。
- 复杂判断返回给人，不硬装自动完成。
- 状态、评价、反思必须落盘，防止人和 AI 忘步骤。
- 多终端交互通过 `03-exchange/` 文件夹完成。
- 使用 `tmp + rename` 和 `locks/` 避免半写半读。

## 记录模板

```text
时间：
决策：
原因：
影响：
是否需要复查：
```
